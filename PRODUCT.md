# Product

## Register

product

## Users

CodeLark 面向在飞书中持续推进本地编码任务的开发者。他们经常离开终端、在移动端或群聊里查看长任务进展，需要快速判断 Agent 做了什么、结果是否可信、是否需要自己介入。

## Product Purpose

CodeLark 把 Codex、Claude Code、Kimi Code 等本地 runtime 的会话、思考、工具调用和最终结果带进飞书。成功意味着用户不打开终端、也不逐层展开卡片，就能理解任务当前状态和关键动作；需要审计时又能展开看到足够具体的命令、路径、查询和输出证据。

## Brand Personality

清晰、克制、可信。界面像一份由工程师写给工程师的实时工作记录：信息密度高，但不堆叠装饰、重复状态或底层协议噪声。

## Anti-references

- 只显示 `exec`、`tool` 等底层包装名，让用户自行猜测实际动作。
- 用图标、颜色、状态词和正文重复表达同一个成功状态。
- 单个工具内部再嵌套“长输出”等折叠；既有的“历史记录 → 工具调用组 → 单工具”信息架构保留。
- 把 JavaScript wrapper、JSON 转义或换行压成一行，当作“工具详情”。
- 不说明遗漏范围的硬截断，或为了完整而把无限长日志塞进卡片。

## Design Principles

1. 先说动作，再说机制：标题回答“对什么做了什么”，内部协议只在详情中出现。
2. 一条信息只表达一次：颜色和图标承担状态，文字承担对象、结果、数量、耗时与异常原因。
3. 渐进披露但不藏结论：折叠前足以扫读，展开后提供可复核证据。
4. 跨 runtime 使用同一语义：Codex、Claude Code、Kimi Code 的不同事件先归一为 read/search/run/edit 等动作，再进入同一渲染器。
5. 可预期地裁剪：长内容展示稳定前缀，并同时遵守字符与行数上限，明确告知遗漏规模。

## Accessibility & Inclusion

状态不能只靠颜色区分，必须同时使用形状稳定的图标；正文和代码保持飞书原生字号与可选中性，不用低对比度装饰文字。所有摘要在窄屏下仍应保留动作和对象，次要元数据可以后移或省略。
